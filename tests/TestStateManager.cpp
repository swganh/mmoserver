#include "gtest/gtest.h"
#include "ZoneServer/StateManager.h"
#include <memory>

namespace {

	TEST(StateManagerTests, CanLoadMaps)
	{
		// create the State Manager Object
		//std::unique_ptr<StateManager> stateManager = std::unique_ptr<StateManager>(new StateManager());

		//EXPECT_EQ(1,stateManager->mActionStateMap.size());
	}

}